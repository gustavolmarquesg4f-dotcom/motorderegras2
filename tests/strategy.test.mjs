import test from 'node:test';import assert from 'node:assert/strict';
import {strategicFindings,OFFICIAL_RESEARCH_SOURCES} from '../shared/strategy.mjs';
const model={plan:{months:60,monthly:1000,mode:'substitution'},payroll:{actualCashAfterLoan:8955,foodBenefit:2300,foodUsed:1800},budget:[{id:'1',name:'Casa',gross:9300,ticket:0}],consignado:{remaining:35,installment:1871.35,confirmedStop:false},debts:[{id:'s',creditor:'Santander',type:'Cartão',base:37509.08,claim:52676.18,mode:'included',kind:'consumer',verified:false}],evidence:{loan:{paidInterest:13834.6,settlementAmount:48855.2,settlementAsOf:'2026-08-04'},ledgers:{s:[{kind:'other_adjustment',amount:8988.05},{kind:'transfer_pending',amount:9500}]}}};
test('strategy flags residual, budget, payroll, dated quote and classified card issues',()=>{const r=strategicFindings(model,new Date('2026-09-25'));for(const key of ['RESIDUAL','CASH_DEFICIT','PAYROLL_CONDITIONAL','LOAN_QUOTE','PAID_INTEREST','UNVERIFIED_CREDITOR','UNDISCLOSED_CHARGES','UNRECONCILED_TRANSFERS','CARD_OPERATION_CAP'])assert.ok(r.issues.some(i=>i.code===key),key)});
test('no historical interest is presented as a refundable credit',()=>{const r=strategicFindings(model);assert.match(r.issues.find(x=>x.code==='PAID_INTEREST').why,/sem abatimento automático/)});
test('official sources have fixed https domains',()=>{for(const s of OFFICIAL_RESEARCH_SOURCES)assert.ok(['www.planalto.gov.br','www.bcb.gov.br','www.tjdft.jus.br','www.gov.br'].includes(new URL(s.url).hostname))});